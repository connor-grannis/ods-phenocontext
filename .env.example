# Copy this file to .env and fill in values. Never commit .env.
# Used only by the teacher group (dev/refresh cycles); not needed at inference time.

AWS_REGION=us-east-2
# AWS_PROFILE=your-profile-name   # optional: named profile from ~/.aws/credentials
