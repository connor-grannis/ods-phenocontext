# Rule system package.
# Rules are loaded from YAML files in rules/ at the repo root.
# Each rule returns per-label confidence scores; rules can abstain.
# Full implementation comes in the modeling roadmap rule-build phase.
